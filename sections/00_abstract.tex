% !TEX root = ../main.tex
\todo{Abstract. One paragraph: reward models as the shared upstream evaluator; we ask whether
EU-protected demographic (and intersectional) biases are inherited, low- vs high-complexity, and
removable by mechanistic reward shaping --- and whether that removal is robust (the reward-vs-representation gap).}
